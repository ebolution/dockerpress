#!/usr/bin/env bash
set -euo pipefail

set -a # Enable export all variables

source ${PROPERTIES_DIR}/color_properties
source ${PROPERTIES_DIR}/dockerpress_properties

ROOT_DIR=$PWD

set +a # Disable export all variables
